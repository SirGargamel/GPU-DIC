float resultVal = 0;           
if (deltaF != 0 && deltaG != 0) {
    for (int i = 0; i < subsetSize2; i++) {            
        resultVal += subsetI[i] * deformedI[i];
    }
    resultVal /= deltaF * deltaG;  
}